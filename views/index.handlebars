<h1>DEM BURGERS!!</h1>

<h2>You needs ta eat these burgs!</h2>

<ul>
  {{#each burgers}}
    {{#unless is_eaten}}
      {{> burgers/burger-block is_eaten=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>You ates these Burgs!</h2>

<ul>
  {{#each burgers}}
    {{#if is_eaten}}
      {{> burgers/burger-block is_eaten=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="is_eaten">Eat this?</label><br>
    <input type="radio" name="is_eaten" value="1" checked> You ates this!<br>
    <input type="radio" name="is_eaten" value="0"> Get ready to eats it!
  </div>

  <button type="submit">Add Burger</button>
</form>
